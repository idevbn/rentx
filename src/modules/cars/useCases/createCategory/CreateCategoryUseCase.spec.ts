describe("Create category", () => {
  it("Hoping that 2 plus 2 is equal to 4", () => {
    const sum = 2 + 2;
    const result = 4;

    expect(sum).toBe(result);
  });

  it("Hoping that 2 plus 2 is not equal to 5", () => {
    const sum = 2 + 2;
    const result = 5;

    expect(sum).not.toBe(result);
  });
});
